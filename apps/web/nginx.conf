server {
    listen 5173;
    root /usr/share/nginx/html;
    index index.html;
    
    # SPA routing - serve index.html for non-file requests
    location / {
        try_files  /index.html;
    }
}
